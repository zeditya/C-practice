#include <stdio.h>
int main () {
  int num = 1;
  while (num <= 10)
  {
     printf("%d\n", num);
     num = num + 1;
  }
  printf ("the numbers are printed and the loop has ended successfully");
  
  return 0;
}